{
  "name": "amnezia-wg-easy",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted AmneziaWG panel",
  "scripts": {
    "build": "DOCKER_BUILDKIT=1 docker build --tag amnezia-wg-easy .",
    "test": "node --test tests/node/*.test.js"
  }
}
